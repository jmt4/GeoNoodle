@model MvcAuth.Models.Job
<div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <p>Category</p>
            @Html.DropDownList("categoryName", new SelectList(Enum.GetValues(typeof(MvcAuth.Models.Category))));<button type="submit" name="category">Submit</button>
            <p> </p>
            <p>Job</p>
            @Html.DropDownList("jobName", new SelectList(ViewBag.JobList));<button type="submit" name="job">Submit</button>
        }
</div>